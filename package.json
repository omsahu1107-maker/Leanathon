{
  "name": "admitai-platform",
  "version": "1.0.0",
  "description": "AdmitAI — Autonomous Student Admission & Conversion Agent | GIET University | SIH Project by Byte Force",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "keywords": [
    "admitai",
    "sih",
    "smart-india-hackathon",
    "edtech",
    "admission-agent",
    "react",
    "express",
    "giet-university",
    "byteforce"
  ],
  "author": "Byte Force — Smart India Hackathon 2026",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
